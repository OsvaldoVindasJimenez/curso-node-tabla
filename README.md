# notas

Este es mi primer programa en Node 
```

Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     La base nos da el valor de la base de la tabla de multiplicar
                 que bamos a ver                             [number] [required]
  -h, --hasta    Esta opcion indica la a[plicacion de multiplos con que se va a
                 multiplicar la base]                     [number] [default: 10]
  -L, --Listar   Con la L de lista desidimos si se va a imprimir la tabla de
                 multiplicar o si de lo contrario solo seva a crear
                                                      [boolean] [default: false]


 nota: este es un archivo tipo Markdown .md y para visualizarlo// view // command pallet // y escribimos: Markdown: Open Preview. y asi se ve la visualizacion de este
```
